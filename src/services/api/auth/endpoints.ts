const base = 'auth/';
export const userLoginEndpoint = `${base}login`;
export const changePasswordEndpoint = `${base}password`;
export const userRegistrationEndpoint = `${base}registration`;
export const getProfileEndpoint = `${base}profile`;
export const refreshTokenEndpoint = `${base}token/refresh`;
